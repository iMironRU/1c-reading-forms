# Инструкции для Claude Code

Книга «Чтение форм 1С» — третья в линии «Чтение 1С» (код → дерево метаданных
→ форма → данные). **Написаны §0.1 и §0.2**, остальные девять модулей не начаты.
Следующий шаг — §0.3, там вводится центральная метафора книги.

Перед любым действием прочитай контекст:

- **`spec/constitution.md`** — метафора разговора, четыре несущих принципа,
  граница метафоры, запреты. Если предложение выходит за рамки конституции —
  откажись от него.
- **`spec/specification.md`** — для кого книга, метрики успеха, пять видов
  упражнений, сквозные примеры, риски.
- **`spec/decisions.md`** — лог решений.
- **`docs/style-guide.md`** — канон серии плюс надстройка книги: усиленная
  парная формулировка, схемы трёх видов, структура параграфа.
- **`docs/00_plan_knigi.md`** — полный план всех 11 модулей.
- **`docs/plan.md`** — текущий статус и ближайший шаг.

---

## Структура репозитория

```
chapters/
  NN_slug/
    NN-MM_slug.md   ← frontmatter обязателен

spec/   ← конституция, спецификация, решения
docs/   ← планы, канон стиля, рабочие процессы
assets/img/
```

Модули: `00_vvedenie`, `01_avtoformy`, `02_anatomiya`, `03_dannye`,
`04_svyaz_s_dannymi`, `05_kompoziciya`, `06_spiski`, `07_komandy`,
`08_komandnyy_interfeys`, `09_osobye_formy`, `10_zavershenie`.

```yaml
---
status: draft      # draft | review | ready
---
```

---

## Четыре несущих принципа (повторяются от Модуля 0 до Модуля 10)

1. **Разговор.** Форма — реплика системы о одном объекте.
2. **Автогенерация.** Не задана форма явно — платформа строит её из метаданных.
3. **Клиент и сервер.** Данные на сервере, пользователь на клиенте; между ними
   ходят данные и запросы, сама форма живёт на клиенте.
4. **Декларативность.** Мы задаём правила, платформа сама строит и оживляет
   интерфейс. «Само сработало» = где-то в дереве задано правило.

---

## Главное правило письма

Каждая метафора немедленно получает **строгую пару с привязкой к редактору** —
вкладка, свойство, действие. Метафора без пары — дефект.

И строго разводим: **реквизит объекта** ≠ **реквизит формы**.

---

## Команды

### `/book-write §N.N [тема]`
Порядок: конституция → спецификация → `docs/00_plan_knigi.md` (раздел модуля) →
соседние параграфы → стиль. `status: draft`.

### `/book-analyze §N.N`
Конституция → спецификация → стиль → консистентность. Отдельно проверяй:
у каждой метафоры есть строгая пара с привязкой к редактору? не смешаны ли
реквизит объекта и реквизит формы? есть ли задание в живом интерфейсе?

### `/book-plan модуль-N`
Уточнить состав модуля по `docs/00_plan_knigi.md`; решения — в `spec/decisions.md`.

---

## Что не делать

- Не писать код обработчиков — ни строки. Обработчики в примерах пусты и помечены.
- Не разбирать `&НаКлиенте` / `&НаСервере` — только концепцию клиент-сервера.
- Не опираться на аналогии с вебом, мобильными или офисными интерфейсами.
- Не оставлять метафору без строгой пары.
- Не изменять `metadata.yaml` напрямую — только через `./book.sh`.
- Не генерировать длинные тексты без запроса — сначала предложи план параграфа.
