# Лист сверки на стенде

*Собран из пометок в тексте скриптом `scripts/stend-checklist.py`. Платформа —
1С:Предприятие 8.3.27, интерфейс «Такси», база «Канцтовары» из «Дерева
метаданных». Отмечайте галочкой и пишите, что вышло: ответ вернётся в текст, а
пометка из него уйдёт.*

## Перед началом

- [ ] Платформа 8.3.27 установлена, записан точный номер сборки.
- [ ] База «Канцтовары» подключена; в ней есть товары, контрагенты, склады,
      поступления № 12 и № 13, реализации и отчёт об остатках.
- [ ] Вариант интерфейса — «Такси», режим совместимости конфигурации записан.
- [ ] Снимки складывать в `assets/img/screens/`, имя — по номеру параграфа:
      `00-01-okno-zapuska.png`.


Всего пунктов: 255 — сверок 204, снимков 51.


# 00_vvedenie


## § 0.1. Платформа в двух режимах: Конфигуратор и Предприятие

- [ ] **снимок** (2. Конфигуратор: где программа устраивается): дерево конфигурации «Канцтоваров» в Конфигураторе, ветки «Общие», «Справочники», «Документы» раскрыты
- [ ] сверка (5. Как изменение доходит до пользователя): команда «Конфигурация → Обновить конфигурацию базы данных» (F7); что видит открытый сеанс Предприятия после обновления — предложение перезапуститься или ничего
- [ ] сверка (7. В режиме Предприятия): формат поставки базы не выбран (каталог файловой базы, dt или cf); шаги ниже написаны для каталога файловой базы
- [ ] **снимок** (7. В режиме Предприятия): окно запуска, база «Канцтовары» выделена
- [ ] сверка (7. В режиме Предприятия): название ярлыка и заголовок окна запуска
- [ ] сверка (7. В режиме Предприятия): точные надписи мастера добавления базы и порядок его шагов
- [ ] сверка (7. В режиме Предприятия): база из поставки открывается без выбора пользователя
- [ ] сверка (7. В режиме Предприятия): как 8.3.27 показывает конфигурацию без подсистем — где общий перечень команд; виден ли пункт «Все функции» в главном меню по умолчанию или включается в параметрах

## § 0.2. Что такое интерфейс пользователя: всё, что видно и трогается

- [ ] сверка (2. Что входит в интерфейс программы): текст вопроса при закрытии изменённой карточки
- [ ] сверка (4. В режиме Предприятия: разные лица одной программы): где в разделе без подсистем команда списка товаров и как она подписана
- [ ] сверка (4. В режиме Предприятия: разные лица одной программы): состав и подписи полей автоформы элемента справочника «Товары»; есть ли поле «Родитель» и как оно подписано; кнопки командной панели
- [ ] **снимок** (4. В режиме Предприятия: разные лица одной программы): карточка товара «Ручка шариковая»
- [ ] сверка (4. В режиме Предприятия: разные лица одной программы): вид номера документа, подписи кнопок командной панели, подпись и колонки табличной части в автоформе документа
- [ ] **снимок** (4. В режиме Предприятия: разные лица одной программы): карточка поступления № 12
- [ ] сверка (4. В режиме Предприятия: разные лица одной программы): подпись отчёта в разделе, надпись кнопки «Сформировать», группировки результата

## § 0.3. Форма как единица интерфейса: разговор о чём-то одном

- [ ] **снимок** (2. Строже: форма в дереве конфигурации): справочник «Товары» в дереве конфигурации, ветки раскрыты
- [ ] сверка (2. Строже: форма в дереве конфигурации): у справочника «Товары» в поставке нет своих форм, ветка «Формы» пуста
- [ ] сверка (3. Пять слов разговора): названия вкладок редактора формы в 8.3.27
- [ ] сверка (4. С чего начинается чтение формы): точные заголовки окон автоформ элемента, документа, списка и отчёта в 8.3.27
- [ ] сверка (6. В режиме Предприятия): заголовок новой карточки документа («Поступление товаров (создание)»?) и кнопка «Создать» в списке
- [ ] сверка (6. В режиме Предприятия): кнопка выбора у ссылочного поля и заголовок окна выбора
- [ ] **снимок** (6. В режиме Предприятия): карточка поступления № 12 с открытым окном выбора контрагента

## § 0.4. Командный интерфейс: оглавление прикладной программы

- [ ] сверка (2. Из чего складывается главное окно): расположение панели разделов и панели функций раздела в «Такси» 8.3.27 по умолчанию; набросок сверить со снимком
- [ ] **снимок** (2. Из чего складывается главное окно): главное окно типовой демонстрационной базы с открытым разделом «Продажи»
- [ ] сверка (4. Что видно в «Канцтоварах»): какие команды 8.3.27 кладёт в «Главное», когда подсистем нет; если списков там нет — путь через «Все функции»
- [ ] сверка (7. В режиме Предприятия): вид панели разделов при одном разделе «Главное»
- [ ] сверка (7. В режиме Предприятия): кнопка «домик» в заголовке формы и панель открытых окон в «Такси» 8.3.27
- [ ] сверка (7. В режиме Предприятия): путь «Главное меню → Вид → Настройка панели разделов»
- [ ] **снимок** (7. В режиме Предприятия): настройка панели разделов «Канцтоваров»

## § 0.5. Клиент и сервер: пользователь здесь, данные там

- [ ] сверка (2. Строже: клиент и сервер — роли): «Изменить…» в окне запуска — как записаны файловая и клиент-серверная база (8.3.27)
- [ ] сверка (7. В режиме Предприятия): второй сеанс файловой базы из поставки открывается без вопросов (разделённый режим)
- [ ] сверка (7. В режиме Предприятия): обновляется ли открытая во втором сеансе карточка без повторного открытия
- [ ] сверка (7. В режиме Предприятия): что покажет второй сеанс, если в нём попробовать изменить товар, который правят в первом, — там запись уже заперта

## § 0.6. Декларативность платформы: не написать действие, а задать правило

- [ ] сверка (3. Одно правило — много последствий): ввод по строке в ссылочном поле по умолчанию в 8.3.27
- [ ] сверка (4. Где искать правило): включена ли проверка заполнения у реквизитов поступления в поставке; как выглядит сообщение и подсветка
- [ ] сверка (5. Где правил не хватает): в поставке у формы поступления нет обработчиков, сумма строки не пересчитывается; в модуле объекта есть процедура проведения
- [ ] сверка (6. В Предприятии и в Конфигураторе): как открыть палитру свойств реквизита в Конфигураторе 8.3.27 (двойной щелчок, «Свойства» в контекстном меню)
- [ ] сверка (6. В Предприятии и в Конфигураторе): вкладка «Нумерация» в окне редактирования документа и флажок «Автонумерация» в 8.3.27
- [ ] **снимок** (6. В Предприятии и в Конфигураторе): палитра свойств реквизита «Контрагент» с типом и проверкой заполнения

## § 0.7. Два режима форм: управляемые и обычные

- [ ] сверка (3. Как отличить одну от другой): как выглядит редактор обычной формы рядом с редактором управляемой в 8.3.27; снимок обоих

## § 0.8. Связь с метаданными и с кодом: где здесь дерево, где здесь язык

- [ ] сверка (5. В Конфигураторе): у «ПоступлениеТоваров» в поставке нет своих форм
- [ ] сверка (5. В Конфигураторе): как открыть модуль объекта в 8.3.27; что именно в нём лежит в поставке

## Ответы к части 0

- [ ] сверка: состав команд «Главного» в поставке
- [ ] сверка: значение свойства «Основной режим запуска» в поставке

# 01_avtoformy


## § 1.1. Автогенерируемая форма: что это и когда срабатывает

- [ ] сверка (1. Форма, которой нет в дереве): закладка «Формы» окна редактирования справочника в 8.3.27, страницы «Основные» и «Дополнительные»
- [ ] **снимок** (1. Форма, которой нет в дереве): закладка «Формы» справочника «Товары» — все основные формы не заданы
- [ ] сверка (2. Когда правило срабатывает): поведение тонкого клиента, когда объекту назначена только обычная форма
- [ ] сверка (8. В Конфигураторе и в Предприятии): как выглядит страница «Основные» у справочника без форм в 8.3.27

## § 1.2. Что попадает в автогенерируемую форму: правила сборки

- [ ] сверка (2. Стандартные реквизиты: то, что есть у всех): состав стандартных реквизитов справочника в автоформе 8.3.27 и как показана пометка удаления
- [ ] сверка (3. Как объект показывает себя в чужом поле): свойство «Основное представление» справочника в 8.3.27 и вид поля при каждом значении
- [ ] сверка (4. Свои реквизиты и их порядок): порядок полей в автоформе элемента — совпадает ли с порядком реквизитов в дереве
- [ ] сверка (5. Тип реквизита решает, как выглядит поле): вид полей разных типов в автоформе 8.3.27
- [ ] сверка (6. Табличная часть становится таблицей): набор кнопок над таблицей в автоформе документа
- [ ] **снимок** (8. В Конфигураторе и в Предприятии): дерево справочника «Товары» и карточка товара рядом

## § 1.3. Виды автогенерируемых форм: список, элемент, выбор, группа

- [ ] сверка (3. Список и выбор: близнецы, но не одно): состав командной панели формы выбора в 8.3.27 (кнопка «Выбрать»)
- [ ] сверка (4. Иногда окно выбора не открывается вовсе): свойство «Быстрый выбор» справочника в 8.3.27 и при каком числе записей платформа показывает список
- [ ] сверка (5. Почему у папки своя карточка): свойство реквизита, разделяющее использование для элемента и для группы, и его название в 8.3.27
- [ ] сверка (7. В режиме Предприятия): как в 8.3.27 открывается группа иерархического справочника и что в её автоформе
- [ ] **снимок** (7. В режиме Предприятия): форма списка и форма выбора одного справочника рядом

## § 1.5. Когда оставляем автоформу, когда делаем свою

- [ ] сверка (3. Дешёвое лечение: поправить дерево): настройка списка пользователем в 8.3.27 и где она сохраняется
- [ ] сверка (8. В режиме Предприятия): как в 8.3.27 пользователь убирает колонку из списка и как называется пункт меню

# 02_anatomiya


## § 2.1. Форма как объект конфигурации: где она живёт в дереве

- [ ] сверка (2. Что лежит внутри формы): состав и названия вкладок редактора форм в 8.3.27, их порядок
- [ ] сверка (5. Имя и синоним: адрес и подпись): палитра свойств формы в 8.3.27 — состав и названия свойств «Имя», «Синоним», «Комментарий»
- [ ] сверка (5. Имя и синоним: адрес и подпись): заголовок автоформы элемента и списка в 8.3.27 — из чего складывается и каким свойством формы переопределяется
- [ ] сверка (7. В Конфигураторе): где в палитре свойств справочника в 8.3.27 лежит группа основных форм и как называются её поля
- [ ] **снимок** (7. В Конфигураторе): палитра свойств справочника «Товары», группа основных форм, поля пустые
- [ ] сверка (7. В Конфигураторе): название ветки общих форм в дереве 8.3.27 и её место среди «Общих»

## § 2.2. Конструктор формы: первое знакомство

- [ ] сверка (1. Конструктор приходит сам): обе дороги в 8.3.27 — названия закладки, кнопки и пункта меню
- [ ] сверка (2. Первый вопрос: какая это форма): состав и названия видов форм в конструкторе справочника 8.3.27; предлагается ли форма группы при выключенной иерархии
- [ ] сверка (2. Первый вопрос: какая это форма): состав видов форм в конструкторе формы документа 8.3.27
- [ ] сверка (2. Первый вопрос: какая это форма): появляется ли в 8.3.27 выбор «управляемая/обычная форма» при режиме редактирования «Управляемое приложение»
- [ ] сверка (3. Откуда конструктор берёт содержимое): названия вкладок редактора формы, где видны реквизиты формы и элементы, в 8.3.27
- [ ] сверка (4. Остальные вопросы): состав полей и отметок первого шага конструктора формы справочника в 8.3.27, что отмечено по умолчанию
- [ ] сверка (6. В Конфигураторе): как в 8.3.27 добавляется форма из ветки «Формы» — кнопка, меню, сочетание клавиш
- [ ] сверка (6. В Конфигураторе): состояние отметки «Назначить форму основной» по умолчанию в 8.3.27
- [ ] **снимок** (6. В Конфигураторе): первый шаг конструктора формы справочника с выбранным видом «Форма элемента справочника»
- [ ] сверка (6. В Конфигураторе): подтвердить, что после создания неназначенной формы карточка товара в Предприятии открывается автоформой

## § 2.3. Редактор формы: три области и предпросмотр

- [ ] сверка (1. Шесть редакторов в одном окне): порядок и названия закладок редактора формы в 8.3.27; все ли шесть видны сразу
- [ ] сверка (3. Реквизиты: чем форма располагает): что показывает редактор реквизитов сразу после конструктора формы элемента справочника в 8.3.27
- [ ] сверка (4. Элементы: из чего сложено окно): виды элементов, доступные в редакторе элементов формы справочника в 8.3.27
- [ ] сверка (5. Команды: что форма умеет): оба пути в 8.3.27 — вынос команды кнопкой и добавление её в редакторе командного интерфейса формы
- [ ] сверка (6. Палитра свойств: где живут настройки): где в 8.3.27 открывается палитра свойств редактора форм и что показывает для корневого элемента «Форма»
- [ ] **снимок** (7. В Конфигураторе): редактор формы элемента справочника «Товары», закладка элементов, снизу предпросмотр
- [ ] сверка (7. В Конфигураторе): чем предпросмотр в редакторе 8.3.27 отличается от окна в Предприятии — данные, командная панель, кнопки

## § 2.4. Назначение формы объекту

- [ ] сверка (1. Где живёт назначение): закладка «Формы» и группа «Основные формы» в окне редактирования справочника 8.3.27 — точные названия строк
- [ ] сверка (1. Где живёт назначение): какие формы 8.3.27 предлагает в строке основной формы элемента — все формы объекта или только подходящие
- [ ] сверка (2. Что значит «основная»): поведение 8.3.27, когда у объекта несколько форм одного вида и основная не назначена
- [ ] сверка (5. Почему пользователь не видит изменений сразу): что делает 8.3.27 с открытым сеансом Предприятия после обновления конфигурации базы данных — предлагает перезапуск или требует
- [ ] **снимок** (6. В Конфигураторе): закладка «Формы» справочника «Товары», в строке основной формы элемента выбрана наша форма
- [ ] сверка (6. В Конфигураторе): отличается ли на вид карточка товара, открытая назначенной формой, от автоформы в 8.3.27

## § 2.5. Запуск в Предприятии: видим то, что собрали

- [ ] сверка (1. Совпадение — это результат): сверить на 8.3.27 карточку товара до и после назначения формы — совпадают ли раскладка, заголовок и командная панель до мелочей
- [ ] сверка (2. Что всё-таки изменилось): проверить на 8.3.27 — добавить справочнику «Товары» реквизит и убедиться, что в назначенной форме элемента он не появился, а в списке появился
- [ ] **снимок** (5. В режиме Предприятия и обратно): карточка товара в Предприятии, открытая назначенной формой
- [ ] сверка (5. В режиме Предприятия и обратно): как в редакторе элементов 8.3.27 меняют порядок — перетаскиванием, стрелками на панели, сочетанием клавиш

## § 2.6. Чужая форма: разбор по трём областям

- [ ] сверка (4. Что выдаёт замысел): примеры каждого признака в конфигурации «Чужие формы» — подобрать при сборке стенда
- [ ] **снимок** (6. Разбор по шагам): форма из конфигурации «Чужие формы», структура элементов с группами
- [ ] сверка (6. Разбор по шагам): выбрать форму для разбора в конфигурации «Чужие формы» и записать ответы на все семь шагов

# 03_dannye


## § 3.1. Реквизит объекта и реквизит формы: одно слово, два смысла

- [ ] сверка (3. Реквизит формы: то, чем форма располагает): как выглядит список реквизитов формы в 8.3.27 — колонки, порядок, что показано для каждого реквизита
- [ ] **снимок** (7. В Конфигураторе): рядом дерево с реквизитами справочника «Товары» и список реквизитов формы
- [ ] сверка (7. В Конфигураторе): что стоит в списке реквизитов формы элемента сразу после конструктора в 8.3.27 — имя, тип, отметка основного

## § 3.2. Основной реквизит: главный предмет разговора

- [ ] сверка (1. Строка, которую завёл конструктор): как в 8.3.27 отмечен основной реквизит в списке реквизитов формы — полужирным шрифтом, отметкой, свойством
- [ ] сверка (2. Имя и тип зависят от вида формы): имена и типы основного реквизита в формах элемента, документа и списка, созданных конструктором в 8.3.27
- [ ] сверка (2. Имя и тип зависят от вида формы): вопрос о сохранении при закрытии изменённой карточки товара в 8.3.27 — точный текст и кнопки
- [ ] сверка (3. Почему форма «знает», кем ей быть): какие формы 8.3.27 предлагает в строках основных форм справочника в зависимости от типа основного реквизита формы
- [ ] сверка (5. Почему основной только один): где в 8.3.27 задаётся, какой реквизит формы основной, — свойство реквизита или отметка в списке
- [ ] **снимок** (7. В Конфигураторе): список реквизитов формы элемента «Товары», основной реквизит «Объект» выделен, открыты его свойства
- [ ] сверка (7. В Конфигураторе): как удаляется форма объекта в 8.3.27 и не требует ли удаление обновления конфигурации базы данных

## § 3.3. Разворачиваем «Объект»: откуда в форме реквизиты товара

- [ ] сверка (1. Что внутри): состав развёрнутого «Объекта» у формы элемента «Товаров» в 8.3.27 — какие стандартные реквизиты видны, в каком порядке
- [ ] **снимок** (1. Что внутри): список реквизитов формы элемента «Товаров», «Объект» развёрнут
- [ ] сверка (4. Путь через точку): название свойства поля с путём к данным в палитре свойств 8.3.27
- [ ] сверка (5. У документа внутри есть и таблица): как показана табличная часть внутри развёрнутого «Объекта» формы документа в 8.3.27
- [ ] сверка (7. В Конфигураторе): появляется ли новый реквизит объекта внутри «Объекта» в открытом редакторе формы сразу или после повторного открытия формы в 8.3.27

## § 3.4. Дополнительные реквизиты формы: данные помимо записи

- [ ] сверка (3. Как заводят): как добавляется реквизит формы в 8.3.27 — кнопка, пункт меню; названия свойств «Имя», «Заголовок», «Тип»
- [ ] сверка (4. Завели — а на экране ничего): какой элемент 8.3.27 создаёт при перетаскивании реквизита формы типа Булево в элементы
- [ ] сверка (6. Два свойства со схожими названиями): значение свойства «Сохраняемые данные» по умолчанию у нового реквизита формы в 8.3.27; где у формы задаётся сохранение данных в настройках и как отмечаются реквизиты
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): список реквизитов формы «Товаров»: «Объект» и «ПоказыватьПодробности» рядом
- [ ] сверка (7. В Конфигураторе и в Предприятии): спрашивает ли 8.3.27 о сохранении при закрытии карточки после переключения флажка реквизита формы
- [ ] сверка (7. В Конфигураторе и в Предприятии): как в 8.3.27 включить сохранение данных формы в настройках и отметить реквизит — свойство формы, колонка или отметка в списке реквизитов

## § 3.5. Типы реквизитов формы: простые, ссылочные, особые

- [ ] сверка: состав окна выбора типа для реквизита формы в 8.3.27 и чем он отличается от окна выбора типа реквизита объекта
- [ ] сверка (4. Особые: таблица, которой нет в базе): как в 8.3.27 задаются колонки реквизита формы типа «Таблица значений» — вложенные строки в списке реквизитов, кнопка добавления колонки
- [ ] сверка (5. Остальные особые типы — кратко): какие ещё особые типы доступны реквизиту формы в 8.3.27 — дерево значений, табличный документ и другие
- [ ] сверка (6. Тип решает, каким будет элемент): какие элементы 8.3.27 создаёт при перетаскивании реквизитов формы разных типов, в том числе таблицы значений
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): реквизит формы «ОстаткиПоСкладам» с раскрытыми колонками в списке реквизитов

## § 3.6. Форма и запись: две жизни одних данных

- [ ] сверка (2. Что происходит при открытии): что показывает 8.3.27 при открытии карточки товара, если запись в это время изменил другой пользователь
- [ ] сверка (3. Правка меняет копию): как в 8.3.27 выглядит попытка открыть на изменение запись, которую правит другой сеанс
- [ ] сверка (5. Закрыли без записи — копии нет): как в 8.3.27 задан режим использования модальности в свойствах конфигурации
- [ ] **снимок** (6. В режиме Предприятия): карточка с изменённым артикулом и список товаров с прежним значением рядом

## § 3.7. Чужая форма: основной реквизит и всё остальное

- [ ] сверка (1. Сначала основной): виден ли тип основного реквизита в списке реквизитов 8.3.27 без открытия свойств
- [ ] **снимок** (6. Разбор по шагам): список реквизитов чужой формы: основной, дополнительные, один без элемента
- [ ] сверка (6. Разбор по шагам): выбрать в конфигурации «Чужие формы» форму с дополнительными реквизитами разных типов и записать ответы на все пять шагов

# 04_svyaz_s_dannymi


## § 4.1. Элемент формы: то, что пользователь видит и трогает

- [ ] сверка (1. Элемент — это то, что видно): полный список видов элементов, доступных в редакторе элементов формы справочника в 8.3.27
- [ ] **снимок** (2. Живут элементы деревом): структура элементов формы элемента «Товаров» с раскрытым корневым элементом «Форма»
- [ ] сверка (3. У каждого элемента своё имя): какое имя даёт 8.3.27 элементу, созданному перетаскиванием реквизита, и можно ли его изменить
- [ ] сверка (5. Свойства: чем элементом управляют): как в 8.3.27 сгруппированы свойства элемента в палитре и какие группы видны у поля
- [ ] **снимок** (7. В Конфигураторе): поле «Артикул» выделено в структуре элементов, справа открыта палитра свойств
- [ ] сверка (7. В Конфигураторе): совпадают ли имена элементов с именами реквизитов в форме, собранной по главе 2 в 8.3.27

## § 4.2. Почему элементы не равны данным

- [ ] сверка (1. Два удаления): что делает 8.3.27 с элементом, у которого удалили реквизит, — предупреждает, очищает путь к данным или оставляет как есть
- [ ] сверка (5. Связь называется путём к данным): название свойства элемента с путём к данным в палитре свойств 8.3.27 и как оно выглядит у поля, связанного с реквизитом объекта
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): карточка товара без поля артикула рядом со списком, где артикул виден
- [ ] сверка (7. В Конфигураторе и в Предприятии): какое место в структуре элементов 8.3.27 отводит возвращённому перетаскиванием полю — в конец или на прежнее место

## § 4.3. Поле и его путь к данным

- [ ] сверка (2. Путь читается по частям): как выглядит значение пути к данным в палитре свойств 8.3.27 у поля артикула и у поля-флажка
- [ ] сверка (3. Путь не пишут, его выбирают): как в 8.3.27 выбирается путь к данным у добавленного вручную поля — список доступных реквизитов, кнопка выбора
- [ ] сверка (4. Из пути поле узнаёт свой тип): что именно 8.3.27 показывает у поля, связанного с реквизитом типа «Дата», и у поля со ссылкой на справочник
- [ ] сверка (5. Когда путь ведёт в пустоту): что делает 8.3.27 с полем, у которого путь ведёт на удалённый реквизит, — сообщение при открытии формы, пустое поле, ошибка проверки конфигурации
- [ ] **снимок** (7. В Конфигураторе): палитра свойств поля «Артикул» с заполненным путём к данным
- [ ] сверка (7. В Конфигураторе): как в 8.3.27 добавляют элемент вручную и где выбирают ему путь к данным

## § 4.4. Виды полей: одно значение, разные способы показать

- [ ] сверка (1. Свойство «Вид» и кто его заполняет): как в 8.3.27 выглядит свойство «Вид» у поля и что стоит в нём по умолчанию у поля, созданного перетаскиванием
- [ ] сверка (2. Пять рабочих видов): полный список видов поля в 8.3.27 и какие из них предлагаются для реквизита строкового типа
- [ ] сверка (3. Вид ограничен типом): какие виды 8.3.27 предлагает в списке у поля, связанного с реквизитом типа Булево, и что будет при попытке выбрать неподходящий
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): список видов у поля «Артикул» в палитре свойств
- [ ] сверка (7. В Конфигураторе и в Предприятии): состав списка видов у поля с реквизитом типа Булево в 8.3.27

## § 4.5. Свойства элемента: видимость, доступность, заголовок, подсказка

- [ ] сверка (1. Четыре свойства, которые есть почти у всех): как эти четыре свойства названы и сгруппированы в палитре свойств поля в 8.3.27
- [ ] сверка (3. Доступность и только просмотр): как в 8.3.27 выглядит поле с Доступность = Ложь и поле с Только просмотр = Истина; можно ли из второго скопировать значение
- [ ] сверка (4. Заголовок и подсказка): варианты отображения подсказки у поля в 8.3.27 и как выглядит каждый
- [ ] сверка (6. Пользователь тоже настраивает форму): путь к настройке формы в 8.3.27 («Ещё» → «Изменить форму…»), точные надписи и кнопка добавления полей
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): настройка формы в Предприятии, в списке видно скрытое поле «Единица измерения»
- [ ] сверка (7. В Конфигураторе и в Предприятии): как в 8.3.27 сбросить пользовательскую настройку формы и что происходит с заголовком поля при пустом свойстве

## § 4.6. Чужая форма: разбор полей и путей к данным

- [ ] сверка (2. Идти по структуре, а не по экрану): как в 8.3.27 пройти по структуре элементов формы сверху вниз и видно ли в ней, у каких элементов снята видимость
- [ ] **снимок** (7. Разбор по шагам): структура элементов чужой формы и рядом таблица разбора с тремя заполненными строками
- [ ] сверка (7. Разбор по шагам): выбрать в конфигурации «Чужие формы» форму с полями разных видов, скрытым элементом и полем без пути; записать разбор целиком

# 05_kompoziciya


## § 5.1. Авторазмещение: у формы нет координат

- [ ] сверка (1. Координат нет): какие свойства размера и положения видны у поля в палитре свойств 8.3.27 и есть ли среди них координаты
- [ ] сверка (3. Чем управляют вместо координат): в чём задаётся ширина элемента в 8.3.27 и что происходит, если места меньше, чем запрошено
- [ ] сверка (4. «Решай сама» — обычное значение): как в палитре свойств 8.3.27 показано незаданное значение растягивания и что стоит в ширине поля по умолчанию
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): карточка товара в широком и узком окне рядом
- [ ] сверка (7. В Конфигураторе и в Предприятии): как в 8.3.27 называется свойство растягивания по горизонтали и что видно на предпросмотре при его включении

## § 5.2. Группы: рамка, внутри которой свой порядок

- [ ] сверка (2. Главное свойство: как выстраивать вложенное): как называется свойство группировки в палитре 8.3.27 и что стоит в нём у новой группы по умолчанию
- [ ] сверка (4. Рамка и заголовок — отдельные решения): как выглядят слабое, обычное и сильное выделение группы в 8.3.27 и где эти свойства в палитре
- [ ] сверка (7. В Конфигураторе и в Предприятии): как в 8.3.27 добавляется обычная группа и какие свойства предлагает палитра сразу после добавления
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): структура элементов формы «Товаров» с группой «Учёт» и двумя полями внутри
- [ ] сверка (7. В Конфигураторе и в Предприятии): поведение группы «горизонтальная, если возможно» в узком окне 8.3.27

## § 5.3. Страницы: форма, которую показывают по частям

- [ ] сверка (1. Две группы, а не одна): как в 8.3.27 добавляется группа «Страницы» и страницы в ней — одной командой или по отдельности
- [ ] сверка (2. Закладки можно показывать по-разному): как в 8.3.27 выглядят закладки сверху и слева и что выбирает платформа при значении «авто»
- [ ] сверка (3. Какая страница открывается первой): как в 8.3.27 задаётся текущая страница у группы «Страницы» в редакторе и какая открывается, если не задано
- [ ] сверка (7. В Конфигураторе и в Предприятии): шаги конструктора формы документа в 8.3.27 и где отметка «назначить основной»
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): структура элементов формы поступления: номер и дата вверху, под ними страницы «Шапка» и «Товары»
- [ ] сверка (7. В Конфигураторе и в Предприятии): где в 8.3.27 свойство положения закладок у группы «Страницы» и как выглядит значение «слева»

## § 5.4. Декорации: то, что в форме ради пользователя, а не ради данных

- [ ] сверка (1. Два вида, и оба без данных): как в 8.3.27 добавляется декорация и где выбирают её вид — надпись или картинку
- [ ] сверка (2. Надпись): названия свойств надписи в палитре 8.3.27 — заголовок, рамка, гиперссылка — и можно ли в заголовке задать форматированный текст
- [ ] сверка (4. Разделителя нет — есть группа): как выглядит группа со слабым выделением в 8.3.27 — есть ли линия под заголовком — и какие свойства задают интервалы между элементами
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): группа «Учёт» в структуре элементов с двумя полями и надписью-пояснением
- [ ] сверка (7. В Конфигураторе и в Предприятии): как меняется вид группы «Учёт» с горизонтальной группировкой, если добавить в неё надпись, — встаёт ли надпись в строку с полями

## § 5.5. От дерева к скетчу и обратно

- [ ] сверка (3. Из дерева в скетч): как в 8.3.27 на самом деле легла надпись-пояснение в горизонтальной группе «Учёт» — в строку с полями или ниже; поправить скетч по стенду
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): дерево элементов формы «Товаров» рядом с карточкой в Предприятии
- [ ] сверка (7. В Конфигураторе и в Предприятии): сверить скетч формы «Контрагентов» из параграфа с тем, что получается в 8.3.27 при описанном дереве

## § 5.6. Чужая форма: композиция и замысел

- [ ] **снимок** (7. Разбор по шагам): дерево элементов чужой формы с раскрытыми страницами и скетч первой закладки рядом
- [ ] сверка (7. Разбор по шагам): выбрать в конфигурации «Чужие формы» форму документа со страницами, скрытой группой и декорацией; сделать разбор целиком и сверить скетч с экраном

# 06_spiski


## § 6.1. Таблица: элемент, который показывает строки

- [ ] сверка (1. Чем таблица отличается от поля): как в 8.3.27 выглядит таблица товаров в форме поступления — колонки, командная панель над таблицей
- [ ] сверка (2. Откуда таблица берёт строки): как выглядит путь к данным у таблицы товаров и у таблицы остатков в палитре 8.3.27
- [ ] сверка (4. Текущая строка): как в 8.3.27 выделяется текущая строка в таблице формы и какие кнопки командной панели действуют на неё
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): структура элементов формы поступления с раскрытой таблицей товаров и выделенной колонкой
- [ ] сверка (7. В Конфигураторе и в Предприятии): как в 8.3.27 удаляется строка табличной части в форме документа — кнопка, клавиша — и спросит ли форма о сохранении при закрытии

## § 6.2. Таблица из табличной части: товары в поступлении

- [ ] сверка (2. Как таблица попадает в форму): создаёт ли 8.3.27 при перетаскивании табличной части колонки для всех её реквизитов и колонку номера строки
- [ ] сверка (2. Как таблица попадает в форму): как в 8.3.27 называется и показывается колонка номера строки в таблице табличной части
- [ ] сверка (4. Сумма документа и сумма строки): после изменения количества в строке и записи поступления в 8.3.27 — сумма документа пересчиталась, сумма строки осталась прежней
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): страница «Товары» в структуре элементов с новой таблицей и её колонками

## § 6.3. Колонки таблицы: поля, которые показывают каждую строку

- [ ] сверка (1. Путь колонки по частям): как выглядит путь к данным у колонки табличной части в палитре 8.3.27 — полный или относительно таблицы
- [ ] сверка (3. Свойства, которые есть только у колонки): как в 8.3.27 включается подвал у таблицы и где у колонки свойства подвала и фиксации
- [ ] сверка (3. Свойства, которые есть только у колонки): как в 8.3.27 вывести в подвал колонки «Сумма» итог по табличной части — путь к данным подвала
- [ ] сверка (5. Группы колонок): как в 8.3.27 выглядит группа колонок в таблице — колонки одна над другой или рядом под общим заголовком
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): таблица товаров на предпросмотре с изменённым заголовком и порядком колонок

## § 6.4. Динамический список: таблица, которая показывает базу

- [ ] сверка (2. Основная таблица: что показывать): как в 8.3.27 выглядят свойства динамического списка в редакторе — основная таблица, флажок произвольного запроса
- [ ] сверка (3. Запрос на сервере, строки на клиенте): включено ли в 8.3.27 динамическое считывание по умолчанию у динамического списка формы списка
- [ ] сверка (4. Почему список нельзя записать): обновляется ли в 8.3.27 открытый список товаров сам после записи карточки из него же и что видит второй сеанс
- [ ] **снимок** (7. В Предприятии): список товаров в Предприятии, командная панель без кнопки записи

## § 6.5. Форма списка: где живёт динамический список

- [ ] сверка (1. Основной реквизит формы списка): что создаёт конструктор формы списка справочника в 8.3.27 — имя основного реквизита, состав элементов, командная панель
- [ ] сверка (2. Что собрал конструктор): какие колонки и какие кнопки командной панели создаёт конструктор формы списка «Товаров» в 8.3.27
- [ ] сверка (4. Иерархия: список с вложенностью): как в 8.3.27 показывает иерархию форма списка «Товаров» по умолчанию и какие значения отображения таблицы доступны
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): реквизиты формы списка «Товаров» с раскрытым основным реквизитом «Список»
- [ ] сверка (7. В Конфигураторе и в Предприятии): как в 8.3.27 добавляется колонка в таблицу формы списка — перетаскиванием поля из раскрытого основного реквизита

## § 6.6. Настройки списка: поля, отбор, порядок

- [ ] сверка (1. Четыре настройки): как в 8.3.27 открываются настройки динамического списка из его свойств и какие закладки там есть
- [ ] сверка (3. Произвольный запрос): где в 8.3.27 включается произвольный запрос у динамического списка и как открывается текст запроса
- [ ] сверка (4. Поля: откуда их больше, чем реквизитов): какие поля предлагает список доступных полей у динамического списка «Товаров» в 8.3.27 и есть ли среди них поля связанных записей
- [ ] сверка (5. Настройки разработчика и настройки пользователя): как в 8.3.27 пользователь ставит отбор в списке и где эта настройка сохраняется
- [ ] сверка (5. Настройки разработчика и настройки пользователя): как в 8.3.27 отбор разработчика включается в пользовательские настройки динамического списка и что тогда видит пользователь
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): настройки динамического списка с заданным порядком по наименованию
- [ ] сверка (7. В Конфигураторе и в Предприятии): как в 8.3.27 задаётся отбор в настройках динамического списка — закладка, выбор поля, вид сравнения

## § 6.7. Чужая форма: какие здесь таблицы и откуда у каждой строки

- [ ] сверка (2. Первый взгляд: путь таблицы): виден ли путь к данным таблицы в палитре свойств 8.3.27 целиком или обрезается по ширине
- [ ] сверка (3. Второй взгляд: тип реквизита): как в списке реквизитов формы 8.3.27 показан тип у реквизита-динамического списка и у таблицы значений
- [ ] **снимок** (7. Разбор по шагам): структура элементов чужой формы с тремя таблицами, палитра свойств с путём к данным и список реквизитов с типами
- [ ] сверка (7. Разбор по шагам): выбрать в конфигурации «Чужие формы» форму документа с табличной частью, таблицей значений и динамическим списком; пройти разбор целиком

# 07_komandy


## § 7.1. Команда: действие, которое форма предлагает сделать

- [ ] сверка (1. Два списка у одной формы): как в редакторе форм 8.3.27 открывается список команд формы — отдельная закладка или ветка рядом с реквизитами
- [ ] сверка (2. Команда — описание действия, а не само действие): полный состав палитры свойств команды формы в 8.3.27 и порядок свойств в ней
- [ ] сверка (3. Команда и кнопка — разные вещи): как в 8.3.27 выглядит свойство кнопки, связывающее её с командой, и как оно называется в палитре
- [ ] сверка (3. Команда и кнопка — разные вещи): доступна ли в 8.3.27 команда формы, у которой нет ни одной кнопки, — попадает ли она в меню «Ещё» и срабатывает ли её сочетание клавиш
- [ ] сверка (5. Команды, которых никто не задавал): полный состав кнопок командной панели автоформы элемента справочника и формы документа в 8.3.27
- [ ] сверка (6. Что список команд говорит о чужой форме): как в 8.3.27 называется в палитре свойство команды об изменении сохраняемых данных и что стоит у стандартных команд
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): редактор формы элемента «Товаров» с открытым списком команд
- [ ] сверка (7. В Конфигураторе и в Предприятии): меняется ли в 8.3.27 надпись на кнопке вслед за заголовком команды или у кнопки свой заголовок

## § 7.2. Три источника команд: платформа, объект, форма

- [ ] сверка (1. Три источника в одной таблице): как в 8.3.27 устроен редактор команд формы — закладки «Команды формы» и «Глобальные команды», где раздел стандартных команд
- [ ] сверка (2. Стандартные: глаголы, которые платформа знает сама): полный список стандартных команд формы документа и таблицы формы в 8.3.27 и где в редакторе команд лежат команды таблицы — среди стандартных, по элементам
- [ ] сверка (3. Команды объекта: живут в дереве, работают в формах): как в 8.3.27 выбирается реквизит-источник параметра, когда в форме несколько реквизитов подходящего типа, — при перетаскивании из раздела параметризуемых команд
- [ ] сверка (3. Команды объекта: живут в дереве, работают в формах): где в 8.3.27 у справочника ветка «Команды» в дереве и как называются группы размещения команд в форме
- [ ] сверка (6. Как узнать источник в чужой форме): как в 8.3.27 палитра свойств кнопки показывает имя команды для стандартной, объектной и собственной команды — точный вид строки
- [ ] **снимок** (7. В Конфигураторе): редактор команд формы поступления с раскрытым разделом стандартных команд

## § 7.3. Кнопка: элемент, который предлагает команду

- [ ] сверка (2. Как кнопка появляется в форме): как в 8.3.27 появляется кнопка при перетаскивании команды в структуру — вид кнопки, заголовок, куда она встаёт
- [ ] сверка (3. Вид кнопки зависит от места): как в 8.3.27 выглядят на экране четыре вида кнопки и какой вид редактор ставит по умолчанию в панели и вне её
- [ ] сверка (4. Что кнопка решает сама): как в 8.3.27 называются в палитре свойства кнопки «Положение в командной панели» и «Кнопка по умолчанию» и их значения
- [ ] сверка (5. Когда связь обрывается): что делает 8.3.27 с кнопками, когда удаляют команду формы, — удаляет вместе с командой или оставляет с пустой связью; ругается ли проверка конфигурации на кнопку без команды
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): структура элементов формы «Товаров» с новой кнопкой «Записать» в группе «Учёт»
- [ ] сверка (7. В Конфигураторе и в Предприятии): как в 8.3.27 выглядит карточка товара с кнопкой «Записать» в группе «Учёт» и в меню «Ещё»

## § 7.4. Командная панель: где собираются кнопки

- [ ] сверка (2. Панель формы: автоматическая): как в 8.3.27 автоматическая командная панель формы показана в структуре элементов и как называется свойство, которым платформа наполняет её сама
- [ ] сверка (3. Панель таблицы: у каждой своя): где в 8.3.27 лежит командная панель таблицы в структуре элементов и как выглядит её свойство положения
- [ ] сверка (3. Панель таблицы: у каждой своя): что по умолчанию лежит в 8.3.27 в контекстном меню таблицы табличной части по сравнению с её командной панелью
- [ ] сверка (4. Меню «Ещё» и порядок внутри панели): какие кнопки 8.3.27 отправляет в меню «Ещё» при положении «Авто» и попадают ли туда кнопки, которые не поместились по ширине
- [ ] сверка (5. Своя панель в любом месте формы): как в 8.3.27 добавить в обычную группу командную панель и как она выглядит на форме
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): структура элементов формы поступления с раскрытой командной панелью формы и панелью таблицы товаров

## § 7.5. Своя команда формы: «Обновить цены»

- [ ] сверка (1. Четыре шага от замысла до кнопки): как в 8.3.27 добавляется команда формы — кнопка на закладке «Команды формы», какое имя получает новая команда по умолчанию
- [ ] сверка (2. Имя для платформы, заголовок для человека): что показывает 8.3.27 на кнопке команды формы с пустым заголовком — имя команды, разбитое на слова, или пусто
- [ ] сверка (3. Действие и пустой обработчик): точные надписи диалога создания обработчика команды в 8.3.27 и какой вариант выбран по умолчанию
- [ ] сверка (3. Действие и пустой обработчик): что делает 8.3.27, если процедуры из свойства «Действие» нет в модуле формы, — ошибка при проверке модуля, при открытии формы или при нажатии кнопки
- [ ] сверка (5. Флажок, который работает без кода): как в 8.3.27 называется в палитре свойство команды об изменении сохраняемых данных и спрашивает ли карточка при закрытии после нажатия команды с включённым свойством
- [ ] **снимок** (7. В Конфигураторе и в Предприятии): закладка «Команды формы» редактора формы «Товаров» с новой командой «ОбновитьЦены»

## § 7.6. Чужая форма: каталог команд

- [ ] **снимок** (7. Разбор по шагам): список команд чужой формы и структура элементов с раскрытыми панелями рядом
- [ ] сверка (7. Разбор по шагам): выбрать в конфигурации «Чужие формы» форму с командами всех трёх источников, командой без кнопки и командой в контекстном меню; пройти разбор целиком

## Ответы к главе 7

- [ ] сверка: удаляет ли 8.3.27 кнопки вместе с командой формы и спрашивает ли об удалении процедуры
